import React from 'react';
import Link from 'gatsby-link';
import Template from '../../components/templates/Hyperanalysis';

export default (props) => {
  return (
    <Template>
    </Template>
  );
};
